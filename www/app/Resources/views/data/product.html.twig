{# app/Resources/views/luck/number.html.twig #}
{% extends 'base.html.twig' %}

{% block body %}
    <h2>{{ product.name }}</h2>
    <p>{{ product.price }}</p>
    <p>{{ product.description }}</p>
{% endblock %}